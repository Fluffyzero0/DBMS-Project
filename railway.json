{
  "build": {
    "env": {
      "APP_ENV": "production"
    },
    "workingDirectory": "backend"
  },
  "start": "php artisan serve --host=0.0.0.0 --port=$PORT",
  "deploy": {
    "workingDirectory": "backend"
  }
}
